% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series_data_manipulation_tbls.R
\name{ts_ip_discharges_tbl}
\alias{ts_ip_discharges_tbl}
\title{Time Series - Inpatient Discharges Tibble}
\usage{
ts_ip_discharges_tbl(.data, .by_time = "month", .date_col)
}
\arguments{
\item{.data}{The data passed in from \code{\link[=ts_ip_discharges_query]{ts_ip_discharges_query()}}}

\item{.by_time}{The choices are "year", "month", "week", "day" defaults to "month"}

\item{.date_col}{The column containing the date variable of interest}
}
\value{
A tibble with the following columns:
\enumerate{
\item date_col
\item value
}
}
\description{
This takes data from the \code{\link[=ts_ip_discharges_query]{ts_ip_discharges_query()}} and makes an internal summary table using \code{timetk::summarise_by_time}
with the following possible choices: "year", "month", "week", and "day", these are checked
inside of the function, if something else is chose an error will be thrown
and the function will exit. It defaults to "month". This function also uses the
\code{timetk::pad_by_time} function with a value of 0 for time periods that have no data.
}
\details{
\itemize{
\item Returns a tibble
\item Expects \code{\link[=ts_ip_discharges_query]{ts_ip_discharges_query()}} as the data argument
}
}
\examples{
\dontrun{
library(healthyR)

data <- ts_ip_discharges_query()

data \%>\%
  ts_ip_discharges_tbl(.by_time = "year")

data \%>\%
  ts_ip_discharges_tbl(.by_time = "month") \%>\%
  ts_plt(.date_col = dsch_date, .value_col = value)

data \%>\%
  ts_ip_discharges_tbl()

data \%>\%
  ts_ip_discharges_tbl(.by_time = "week") \%>\%
  save_to_excel(.file_name = "ip_discharges")
}

}
\author{
Steven P. Sanderson II, MPH
}
