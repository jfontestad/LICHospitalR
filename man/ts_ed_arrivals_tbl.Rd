% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series_data_manipulation_tbls.R
\name{ts_ed_arrivals_tbl}
\alias{ts_ed_arrivals_tbl}
\title{Time Series - ED Arrivals Tibble}
\usage{
ts_ed_arrivals_tbl(.data, .by_time = "month", .date_col)
}
\arguments{
\item{.data}{The data passed in from \code{\link[=ts_ed_arrivals_query]{ts_ed_arrivals_query()}}}

\item{.by_time}{The choices are "year", "month", "week", "day", "hour", "min" defaults to "month"}

\item{.date_col}{The column containing the date variable of interest}
}
\value{
A tibble with the following columns:
\enumerate{
\item date_col
\item value
}
}
\description{
This takes data from the \code{\link[=ts_ed_arrivals_query]{ts_ed_arrivals_query()}} and makes an internal summary table using \code{timetk::summarise_by_time}
with the following possible choices: "year", "month", "week", "day", and "min", these are checked
inside of the function, if something else is chose an error will be thrown
and the function will exit. It defaults to "month". This function also uses the
\code{timetk::pad_by_time} function with a value of 0 for time periods that have no data.
}
\details{
\itemize{
\item Returns a tibble
\item Expects \code{\link[=ts_ed_arrivals_query]{ts_ed_arrivals_query()}} as the data argument
}
}
\examples{
\dontrun{
library(healthyR)

data <- ts_ed_arrivals_query()

data \%>\%
  ts_ed_arrivals_tbl(.by_time = "year")

data \%>\%
  ts_ed_arrivals_tbl(.by_time = "month") \%>\%
  ts_plt(.date_col = dsch_date, .value_col = value)

data \%>\%
  ts_ed_arrivals_tbl()

data \%>\%
  ts_ed_arrivals_tbl(.by_time = "week") \%>\%
  save_to_excel(.file_name = "ed_arrivals")
}

}
\author{
Steven P. Sanderson II, MPH
}
